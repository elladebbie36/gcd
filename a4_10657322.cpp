#include <iostream>
using namespace std;
int Gcd;
void main ()
{
	int a,b, result;
	printf (Enter the 2 numbers to find the Gcd:);
	scanf(%d %d ; 8a, 2b);
	result= gcd (a,b)
	printf(the Gcd of % and % is %d/n,a,b,result);
	while (a!,b)
	{
		if (a>b)
		{
			return Gcd (a^-b,b);
		}
		else
		{
			return Gcd (a,b,-a);
		}
	}
	return a;
}
